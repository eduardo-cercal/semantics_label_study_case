name: semantics_label_study_case
description: A Semantics Label study case.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mocktail: ^0.3.0
  faker: ^2.1.0
  get: ^4.6.5
  equatable: ^2.0.5
  localstorage: ^4.0.1+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
